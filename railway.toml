[build]
builder = "NIXPACKS"
buildCommand = "pip install -r server/requirements.txt"

[deploy]
startCommand = "cd server && gunicorn --workers 2 --bind 0.0.0.0:$PORT server:app"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
